# Screenshots

Screenshots will be added after the next manual walkthrough pass.

When the LingoPilot bot has run, route screenshots may also appear in the repo root `screenshots/` folder (auto-committed by automation).

To capture locally after running the app:

```bash
npm run dev
# Open http://localhost:3001 and capture each route:
# / · /products · /checkout · /product/1
# Save captures here for README embedding.
```
